# 0.0.7 ← 0.0.8 Bring-forward — implement deferred items in 0.0.7

**Version:** 0.0.7 (no bump)
**Date:** 2026-07-05
**Status:** approved (build mode)
**Decisions locked:**
- Item 3 (vec0): **full cutover, drop ChromaDB** (per @SOTAbriefing research,
  Jul 2026 — sqlite-vec brute-force is 10–30 ms at ≤10 K vectors @ 1024-dim,
  negligible vs BGE-M3 embedding + cross-encoder; ChromaDB 1.5.x has a silent
  HNSW corruption bug; dropping it removes ~23 MB deps + RAM and aligns with
  the single-SQLite-DB philosophy). No dual-write.
- Item 1 (P3 Chat UX): implement **pulse indicator + collapsible tool panels**
  only. Browser-tab title/favicon dynamic updates are **out of scope** (user
  decision). Auto-scroll via IntersectionObserver is already done.
- Item 4 (AGENT_LABELS): frontend parses the `/api/agents` **dict**; canonical
  `maestro → "Tutor"`, `system_support → "Support"` (backend wins).
- Item 5 (CI): new `ci.yml` on push/PR (pytest + Vitest) **and** gate
  `publish.yml` via `needs: [test]`. Run tests with `COGNITS_DISABLE_RAG=1`
  (no ~2.3 GB BGE-M3 download; cutover validated manually in dev).
- Item 6 (Phases 4–8 specs): write 5 retrospective spec docs.

## Context

The "Known deferred items (0.0.7 → 0.0.8)" section of `AGENTS.md` lists six
items. This spec folds them into 0.0.7. Current state (audited by `explore`):

| # | Item | Current state |
|---|------|---------------|
| 1 | P3 Chat UX | Auto-scroll done; pulse + collapsible missing (`Chat.tsx:134-155` flat, no toggle) |
| 2 | GrepCode/GlobFiles `os.walk` | `tool_files.py:316,412` block the event loop; `ReadFile`/`ListDir` show the `asyncio.to_thread` fix |
| 3 | vec0 / ChromaDB→sqlite-vec | Schema at `database.py:209-221`; `vector_index/search/count` (`:356-398`) are dead code; ChromaDB `upsert`/`query` active in `engine.py`; latent bugs: `INSERT OR REPLACE` (`:362,370`), non-deterministic `hash()` rowids (`:401`) |
| 4 | AGENT_LABELS from `/api/agents` | Hardcoded `chat-store.ts:19-31` (drift vs `constants.py:166-178`); `settings-store.ts:138` does `Array.isArray` on a dict → no-op |
| 5 | CI test workflow | Only `publish.yml`; runs `typecheck`+`build`+`uv publish`, no `pytest`, no Vitest |
| 6 | Phases 4–8 specs | `docs/specs/` has `phase-0`–`phase-3` + `0.0.7-hardening.md`; Phases 4–8 implemented but unspecced |

## Execution model

- One atomic task per message (T1 → T7).
- After each code change: `COGNITS_DISABLE_RAG=1 uv run python -m pytest -q`
  + `cd frontend && bun run typecheck` (and `bun run test` where frontend
  tests exist). RAG-enabled validation is manual via `scripts/dev.sh`.
- Commit after each logical unit (AGENTS.md workflow).
- DB schema rule: `SCHEMA_VERSION` stays at 1; new schema changes are
  idempotent `ALTER TABLE`/`DROP … IF EXISTS` checks inside the
  `if version < 1:` block in `_migrate()`; update `BASE_SCHEMA`.

## T1 — GrepCode/GlobFiles async (small)

**Files:** `src/cognits/agent/tool_files.py`
**Changes:**
- Extract the `os.walk` loop + file reads of `GrepCode.execute` (`:316-349`)
  into a module-level `_grep_code_sync(dir_path, compiled, include, max_results)`
  and call `results = await asyncio.to_thread(_grep_code_sync, …)` from
  `execute` (`:288`).
- Extract the `os.walk` loop of `GlobFiles.execute` (`:412-418`) into
  `_glob_files_sync(dir_path, pattern)` and `await asyncio.to_thread(…)`.
- Mirror the canonical pattern already used by `ReadFile` (`:143,150`) and
  `ListDir` (`:244`) in the same file.
**Verify:** `uv run pytest tests/test_tool_files.py -x`
**Acceptance:** no sync `os.walk`/file-read remains inside an `async def
execute`; existing `test_grep_code`/`test_glob_files` pass.

## T2 — AGENT_LABELS from `/api/agents` (small)

**Files:** `frontend/src/stores/chat-store.ts`, `frontend/src/stores/settings-store.ts`
**Changes:**
- `chat-store.ts:19-31`: replace the hardcoded `AGENT_LABELS` const with a
  signal `const [agentLabels, setAgentLabels] =
  createSignal<Record<string,string>>({"": "Agent"})` populated once via
  `fetch("/api/agents").then(r => r.ok ? r.json() : {}).then(setAgentLabels)`.
  Update the single usage at `:211` to
  `agentLabels()[data?.agent] ?? data?.agent ?? "Agent"`. Keep `"" → "Agent"`
  as a local fallback (not a backend key).
- `settings-store.ts:136-141`: the endpoint returns a **dict** `{id: label}`,
  not an array. Parse with
  `Object.entries(list).map(([id, name]) => ({ id, name: String(name), systemPrompt: "" }))`
  when `list && typeof list === "object" && !Array.isArray(list)`. This fixes
  the broken Settings agent registry (currently only ever shows "Orchestrator").
- No backend change: `/api/agents` (`routes_misc.py:89-91`) already returns the
  canonical `AGENT_LABELS` from `constants.py:166-178` (`maestro → "Tutor"`,
  `system_support → "Support"`). The frontend now consumes it, eliminating the
  Teacher/Tutor and System Support/Support drift.
**Verify:** `uv run pytest tests/test_routes_misc.py -x` + `cd frontend && bun run typecheck && bun run test`
**Acceptance:** `chat-store` and `settings-store` labels come from the backend
single source of truth; Settings agent dropdown lists all personas.

## T3 — P3 Chat UX: pulse + collapsible tool panels (small–medium)

**Files:** `frontend/src/components/Chat.tsx` (`:134-155`), `frontend/src/index.css`
(possibly), `chat-store.ts` (if exposing an active-agent memo).
**Changes:**
- **Active-agent pulse indicator:** render a colored dot with `animate-pulse`
  next to the active agent name in the tool-status banner, shown when
  `isStreaming()` and the status ends with `…` (i.e. a tool is running). Use
  the theme accent color.
- **Collapsible tool panels:** wrap the tool-status block in a toggle. Add a
  signal `const [toolsCollapsed, setToolsCollapsed] = createSignal(false)`.
  Expanded (default): show the current per-agent lines. Collapsed: show a
  one-line summary `"{N} herramientas · {agenteActivo}"` with a chevron.
  Clicking the header toggles. Use `<Show>`/`classList` (SolidJS idioms, no
  prop destructuring).
- Defaults (confirm at execution time): **expanded by default**; pulse dot in
  theme accent. No new dependencies.
**Verify:** `cd frontend && bun run typecheck && bun run test` + smoke via
`scripts/dev.sh` (trigger a `web_researcher` deploy to see the banner).
**Acceptance:** tool panels collapse/expand; a pulsing dot marks the active
agent during tool execution.

## T4 — CI: `ci.yml` + gate `publish.yml` (small)

**Files:** new `.github/workflows/ci.yml`, edit `.github/workflows/publish.yml`
**Changes:**
- `ci.yml` triggers `on: [push, pull_request]`. Two jobs:
  - `test-backend`: `actions/checkout@v6`, `astral-sh/setup-uv@v6`
    (`enable-cache: true`), `uv python install 3.13`,
    `COGNITS_DISABLE_RAG=1 uv run python -m pytest -q`.
  - `test-frontend`: `actions/checkout@v6`, `oven-sh/setup-bun@v2`
    (`bun-version: 1.3.13`), `bun install --frozen-lockfile`,
    `bun run typecheck`, `bun run test` (=`vitest run`, confirmed in
    `frontend/package.json:11`).
- `publish.yml`: add a `test` job (same two steps) and make the existing
  `publish` job depend on it via `needs: [test]`, so a tag push publishes
  only if tests are green.
**Verify:** YAML validity (`actionlint` if available, else `python -c
"import yaml; yaml.safe_load(open(p))"`).
**Acceptance:** push/PR runs pytest + Vitest; `v*` tags publish only on green
tests.

## T5 — vec0 cutover / drop ChromaDB (large) — sub-commits

### T5a — Fix latent bugs in `database.py` vector code
**Files:** `src/cognits/storage/database.py`
**Changes:**
- `vector_index` (`:356-374`): `INSERT OR REPLACE` →
  `INSERT ... ON CONFLICT DO UPDATE` for both `report_chunks` and `chunks_vec`
  (restores the storage invariant).
- `_chunk_id_to_int` (`:401-402`): replace `abs(hash(...)) % (2**62)` with a
  deterministic `hashlib.sha1`-based fold (Python `hash` is randomized per
  process → rowids were unstable across runs).
- `chunks_vec` (`:219-221`): recreate with `distance_metric=cosine`. The table
  is dead/empty, so add an idempotent `DROP TABLE IF EXISTS chunks_vec` +
  recreate (with cosine) inside the `if version < 1:` block of `_migrate()`.
  Keep `SCHEMA_VERSION = 1`; update `BASE_SCHEMA`.
- `vector_search` (`:376-394`): rewrite to the vec0 KNN `MATCH` syntax using
  the table's declared cosine metric (not `vec_distance_L2`); fix the join to
  `report_chunks` and the return shape.
- Extend `vector_index` to accept embeddings (list of float lists) alongside
  chunk dicts, so `engine.py` can pass the BGE-M3 vectors through.
**Verify:** `uv run pytest tests/test_database.py -x`

### T5b — Wire `engine.py` to vec0
**Files:** `src/cognits/rag/engine.py`, `src/cognits/storage/database.py` (signature)
**Changes:**
- Thread the `Database` reference into `RagEngine` (mirror how `reports_repo`
  is wired today).
- `_index_sync` (`:345-362`): replace `self._collection.upsert(...)` with
  `db.vector_index(chunks_with_embeddings)`. Embeddings are computed at
  `:352` (`self._worker_embed(texts)`) — pass them through.
- `_search_sync` (`:320-343`): replace `self._collection.query(...)` with
  `db.vector_search(query_embedding, max_results)`; adapt the return shape to
  what `search_hybrid` (`:266-292`) consumes (ids, documents, metadatas,
  distances). Keep FTS5 BM25 + RRF + cross-encoder reranker unchanged.
**Verify:** `uv run pytest tests/test_rrf.py tests/test_tool_rag.py -x`

### T5c — Drop ChromaDB + cleanup
**Files:** `pyproject.toml`, `src/cognits/rag/engine.py`,
`src/cognits/server/app.py`, `src/cognits/paths.py` (if applicable), tests.
**Changes:**
- `pyproject.toml`: remove `chromadb==1.5.9`; pin `sqlite-vec==0.1.9`
  (pre-v1 caveat from research).
- `engine.py`: remove `import chromadb`, `PersistentClient` init
  (`:177-184`), `COLLECTION_NAME`, and all collection management.
- `server/app.py` / `paths.py`: grep for `chroma` references; remove ChromaDB
  directory init/cleanup in `.cognits/`.
- Update tests that mock ChromaDB (`test_rrf.py`, `test_tool_rag.py`,
  `test_app.py`) to mock the vec0 path instead.
**Verify:** `COGNITS_DISABLE_RAG=1 uv run python -m pytest -q` (full suite,
RAG disabled). Side-by-side validation of the dense path manually in dev.
**Acceptance (whole T5):** ChromaDB dependency removed; `chunks_vec`/vec0 is
the sole dense store; hybrid search uses FTS5 + vec0 + RRF + reranker; suite
green; no `chroma` references remain in `src/`.

## T6 — Retrospective specs Phases 4–8 (medium, docs only)

**Files:** `docs/specs/phase-4-mastery.md`, `phase-5-sqlite-vec.md`,
`phase-6-hybrid-search.md`, `phase-7-pedagogy-engine.md`,
`phase-8-frontend.md`.
**Source:** git log + `AGENTS.md`. Format matches `phase-0`–`phase-3`.
**Contents:**
- Phase 4 — graduated mastery + adaptive thresholds + faded scaffolding
  (commit `98c97d4`).
- Phase 5 — sqlite-vec infrastructure for RAG (`681ef0d`, `fb652fc`).
- Phase 6 — hybrid search: RRF fusion + cross-encoder reranker + chunker v2
  (`d135a22`, `8f1aa15`, `416810b`, `251d133`).
- Phase 7 — PedagogyEngine external stage management (`847e0d4`).
- Phase 8 — frontend: API wrapper, SSE types, learner model UI, markdown
  tests, Vitest (`f1c1090`, `5f487ea`, `87b6232`).
**Verify:** docs only (no tests).
**Acceptance:** five spec files exist, each describing its phase's design
decisions and final state.

## T7 — AGENTS.md reconciliation

**Files:** `AGENTS.md`
**Changes:**
- Update "Known deferred items (0.0.7 → 0.0.8)" → mark items 2, 4, 5, 6 done
  in 0.0.7; item 1 partially done (pulse + collapsible done; title/favicon
  remain explicitly out of scope); item 3 done (cutover complete).
- Stack section: RAG line → sqlite-vec vec0 is the active dense store (no
  longer "migrating"; ChromaDB removed).
- RAG hybrid search invariant: `search_hybrid` uses vec0 dense (not
  ChromaDB `collection.query`).
- Remove the "vec0 dual-write / ChromaDB→sqlite-vec cutover" deferred entry.

## Order

T1 → T2 → T3 → T4 → T5a → T5b → T5c → T6 → T7.

## Out of scope (still deferred)

- Browser-tab title/favicon dynamic updates during tool execution (user
  decision; only pulse + collapsible implemented).
- chunker v2 fence-aware section split + H1/H3 boundaries (v2 dead code).
- RAG `rerank_cross_encoder` model caching.
- vec0 ANN (IVF/DiskANN) — sqlite-vec is brute-force only as of v0.1.x;
  revisit if a single user accumulates >50 K chunks.
